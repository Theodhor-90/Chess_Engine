- **`crates/engine/Cargo.toml`** (modified) — Added `[features]` section with `simd-sse2`, `simd-avx2`, `simd-neon` forwarding to `chess-nnue` dependency
- **`.github/workflows/release.yml`** (created) — GitHub Actions workflow with `workflow_dispatch` trigger, 8-entry build matrix covering all 5 target triples × SIMD levels, Rust toolchain setup, Cargo cache, `cross` installation for `aarch64-unknown-linux-gnu`, preflight checks (`fmt`, `clippy`, `test`) on non-cross entries, release builds with `--features` flag, and artifact upload with platform-appropriate binary path (`engine` vs `engine.exe`)