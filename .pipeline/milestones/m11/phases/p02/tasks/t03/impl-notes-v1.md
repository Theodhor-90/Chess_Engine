- **`lichess-bot/config.yml`** — Created. Contains `token` placeholder, `engine` section (dir, name `"engine"`, protocol `"uci"`, UCI options: EvalFile, EvalMode, BookFile, SyzygyPath), `challenge` section (concurrency, sort_by, accept_bot/accept_human, rating range, variants: standard, time_controls: bullet/blitz/rapid/classical, modes: casual/rated).
- **`lichess-bot/README.md`** — Created. Covers: Prerequisites, Create a Lichess Bot Account (with `curl` upgrade command), Obtain an API Token (`bot:play` scope), Install lichess-bot (`pip install` and clone options), Configure (copy engine binary + nn.nnue, set token), Run the Bot (`lichess-bot -c config.yml`), Verify (check profile, send challenge, confirm games).