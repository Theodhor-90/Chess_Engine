- **`crates/nnue/src/simd.rs`** — Fixed test compilation error: `inp.clamp(0, CLIPPED_RELU_MAX)` → `(*inp).clamp(0, CLIPPED_RELU_MAX)` (dereference `&i16` before calling `clamp`); `cargo fmt` applied to collapse multi-line `#[cfg(all(...))]` attributes to single lines per rustfmt rules
- **`crates/nnue/src/inference.rs`** — Removed unused imports `L2_SIZE` and `QUANTIZATION_SHIFT_HIDDEN` from test module; `cargo fmt` applied to fix line-wrapping on `dot_i8_i32(...)` call; fixed `forward_perspective_swap` test saturation bug (accumulator values now differ between perspectives: white=1, black=2, avoiding L2 clipped-ReLU saturation that made both results identical)
- **`crates/nnue/src/accumulator.rs`** — Removed unused imports `NUM_COLORS`, `NUM_PIECE_KINDS`, `NUM_SQUARES` from test module