{"verdict":"needs_revision","feedback":"The plan file is catastrophically incomplete. It contains only 16 lines — the tail end of sections 7 (Verification Commands) and 8 (Design Deviations). The entire body of the plan is missing: sections 1 (Deliverables), 2 (Dependencies), 3 (Implementation Details), 4 (API Contracts), 5 (Test Plan), and 6 (Implementation Order) are all absent. This appears to be a truncation error during plan generation. The plan must be regenerated in full before it can be reviewed.","issues":[{"category":"completeness","description":"The plan file contains only 16 lines total. It starts mid-stream with shell commands from section 7 (Verification Commands) and includes section 8 (Design Deviations: None). Sections 1 through 6 are entirely missing. Compare with the T01 plan-locked.md which has 222 lines covering all 8 sections. The plan must be regenerated from scratch."},{"category":"completeness","description":"Missing section 1 (Deliverables): No listing of files to create or modify, no description of the `unmake_move` method signature or the bitboard/occupancy update operations."},{"category":"completeness","description":"Missing section 2 (Dependencies): No statement of which prior tasks, crates, or types this task depends on (e.g., UndoInfo from T01, Position mutation methods)."},{"category":"completeness","description":"Missing section 3 (Implementation Details): No algorithm description for unmake_move. The implementer needs a step-by-step algorithm covering each move type reversal (quiet, capture, double pawn push, castling, en passant, promotion, promotion-capture) — analogous to the 13-step make_move algorithm in the T01 plan."},{"category":"completeness","description":"Missing section 4 (API Contracts): No usage example showing how unmake_move is called with a Move and UndoInfo to restore position state."},{"category":"completeness","description":"Missing section 5 (Test Plan): No test matrix. The task spec requires round-trip identity tests for every move type and FEN round-trip tests, but no tests are described in the plan. Without this, an implementer cannot write the tests."},{"category":"completeness","description":"Missing section 6 (Implementation Order): No sequencing of implementation steps."},{"category":"format","description":"The plan file appears to be a truncation artifact — it starts with bare shell commands without any markdown heading or preamble, suggesting the first ~200 lines were lost during file creation."}]}