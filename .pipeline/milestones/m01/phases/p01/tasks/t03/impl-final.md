- **`crates/types/src/chess_move.rs`** — Created. `MoveFlag` struct with 14 associated `u16` constants (all 16 flag values, skipping reserved 6/7). `Move` newtype wrapping `u16` with `new`, `from_raw`, `raw`, `from_sq`, `to_sq`, `flags`, `is_capture`, `is_promotion`, `promotion_piece`, `is_castling`, `is_en_passant`, `is_double_pawn_push`. `Display` impl producing UCI notation with promotion suffix. 13 unit tests covering all flag types, boundary squares, flag distinctness, and display output.
- **`crates/types/src/lib.rs`** — Modified. Added `pub mod chess_move;` and `pub use chess_move::{Move, MoveFlag};`.