{"verdict":"needs_revision","feedback":"The plan is well-structured with sound design decisions (trait-based prober, combined probe_root, parameter threading). Three issues need resolution before locking.","issues":[{"category":"ambiguity","description":"Section 3, Root tablebase filtering, step 4: 'If exactly one move remains, return it immediately. Otherwise, continue search with only the filtered moves as a hint (or just set the best as root move).' The parenthetical '(or just set the best as root move)' presents two alternative approaches without choosing one. Specify the exact behavior: either (a) restrict the search to only the filtered moves, or (b) pass the DTZ-best move as the initial PV hint and let the search consider all non-losing moves. Pick one and remove the alternative."},{"category":"ambiguity","description":"Section 3, In-search WDL probing: 'Convert result to an exact score (mate-scale for wins/losses, 0 for draws) and return immediately' and Section 4 API Contracts: 'the value is a converted score (TB_WIN_SCORE, 0, -TB_WIN_SCORE, 1, -1)'. The task spec says 'Adjust mate-scale scores for ply distance to ensure correct ordering in search,' but the plan does not specify whether ply-distance adjustment happens inside the TbProber implementation or in the negamax caller after receiving the score. This matters because TB_WIN_SCORE (29800) is a fixed value from T01's wdl_to_score, but storing unadjusted mate-scale scores in the TT could cause incorrect mate-distance ordering. Specify exactly where and how ply adjustment is applied (e.g., 'negamax adjusts the returned score by subtracting ply before returning' or 'no ply adjustment needed because TB scores are below MATE_SCORE range')."},{"category":"completeness","description":"Test Plan is missing a test for verification criterion 4: 'Probing is correctly gated: no probing occurs when piece count exceeds the configured limit.' While piece-count gating is delegated to the TbProber implementation, the plan should include a test that uses a mock prober which tracks whether probe_wdl was called, and runs negamax on a position with many pieces to verify probe_wdl is never invoked — OR explicitly document that this criterion is covered by T01's SyzygyTablebase tests and is not the search crate's responsibility. Either way, the verification mapping needs to be explicit."}]}