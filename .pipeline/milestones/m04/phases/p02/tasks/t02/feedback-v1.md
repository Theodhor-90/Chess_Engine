{"verdict":"needs_revision","feedback":"The plan incorporates significant T03 scope (depth limits, node limits, ponder support) into what should be a focused T02 time-budget integration task. The T02 spec explicitly scopes to: (1) replacing the hardcoded 5s with allocate_time(), (2) ensuring check_time enforces the budget, (3) ensuring aborted searches return the last fully completed iteration's best move. The SearchLimits struct with max_depth/max_nodes and the corresponding tests belong to T03.","issues":[{"category":"scope","description":"SearchLimits struct with max_depth and max_nodes fields is scope creep from T03. T02's spec says 'Modify the search to use the time allocation from T01 as its time budget' — it does not mention depth or node limits. The current search() already accepts Duration; T02 only needs the engine to call allocate_time() instead of Duration::from_secs(5). The signature change to support depth/nodes limits belongs to T03 ('UCI go variants — support depth, nodes, movetime, infinite, ponder modes')."},{"category":"scope","description":"Test 3 (depth_limit_stops_at_exact_depth) and Test 4 (node_limit_aborts_search) test T03 exit criteria, not T02 exit criteria. T02's verification criteria are: (1) search aborts on time budget expiry, (2) returns valid best move from last completed iteration, (3) hardcoded 5s replaced, (4-6) cargo test/clippy/fmt pass. Remove these tests from T02's plan."},{"category":"scope","description":"The Go handler code in section 3b handles params.ponder (setting Duration::from_secs(86400) for ponder mode). Ponder support is explicitly T03 scope per the phase spec: 'T03 — UCI go variants... ponder (search on the opponent's time; respond to ponderhit by switching to normal time management, respond to stop by returning bestmove)'. Remove ponder handling from T02."},{"category":"scope","description":"The Go handler code in section 3b handles params.depth and params.nodes (setting max_depth and max_nodes from GoParams). These are go variants that belong to T03 per the phase spec. T02 should only wire up time-based allocation."},{"category":"ambiguity","description":"Section 3a presents two approaches for SearchContext modification: 'Replace time_budget: Duration field with limits: SearchLimits (or keep time_budget and add max_nodes). The simplest approach: store max_nodes: Option<u64> alongside time_budget in SearchContext.' The plan must commit to one approach — '(or ...)' and 'The simplest approach' leave the implementer guessing which to use."},{"category":"correctness","description":"The deviation justification claims 'Adding these as separate parameters... would clutter the signature' but this reasoning is speculative — the current search() already accepts Duration and T02 only needs the caller to compute a better Duration. The deviation introduces unnecessary complexity for T02. If T03 later needs SearchLimits, T03's plan should introduce it."}]}