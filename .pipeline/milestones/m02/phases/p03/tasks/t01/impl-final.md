- **`crates/movegen/src/castling.rs`** (created) — Castling move generation module with `generate_castling_moves(pos: &Position, moves: &mut Vec<Move>)`, path clearance bitboard constants, king transit square arrays, match-on-Color logic for all four castling types, and 9 unit tests covering both colors, blocked by pieces, blocked by attacked origin/transit/destination, and revoked rights. Fixed plan's `test_castling_blocked_by_attacked_transit` FEN (bishop on F4 doesn't attack F1 through pawn wall) — replaced with knight on G3 which genuinely attacks F1.
- **`crates/movegen/src/lib.rs`** (modified) — Added `pub mod castling;` declaration and `pub use castling::generate_castling_moves;` re-export.