{"verdict":"needs_revision","feedback":"The check extension implementation in negamax is correct: the `check_extension_enabled` field was added to `SearchContext`, `in_check` was moved before the TT probe, and `depth = depth.saturating_add(1)` is applied when the side to move is in check. The three new tests (`check_extension_activates_in_check`, `check_extension_no_effect_when_not_in_check`, `check_extension_improves_wac_solve_rate`) all pass. However, there are two issues that need fixing.","issues":[{"file":"crates/search/src/lib.rs","severity":"critical","description":"The pre-existing test `futility_does_not_prune_in_check` (line 2456) now fails. It asserts that node counts are identical with futility enabled vs disabled when the root is in check. With check extension enabled (both runs have `check_extension_enabled: true`), the root depth is extended from 3 to 4, causing futility pruning to legitimately activate in deeper non-check child nodes, producing different node counts (4524 vs 4926). The spec requires 'All existing tests continue to pass'. Fix: either disable check extension in this test (set `check_extension_enabled: false` in both SearchContext instances at lines 2479 and 2517), or adjust the test to account for the interaction."},{"file":"crates/search/src/lib.rs","severity":"major","description":"`cargo fmt --check -p chess-search` fails. The new test code at lines 3027-3029 and 3052-3054 and 3087-3089 and 3112-3114 has negamax call arguments on a single line, but rustfmt expects each argument on its own line. The spec requires `cargo fmt --check` to pass. Run `cargo fmt -p chess-search` to fix."}]}