{"verdict":"needs_revision","feedback":"7 out of 8 SEE unit tests fail. The core `see()` function produces incorrect exchange evaluations. The `attackers_to` function and/or X-ray discovery logic has systematic bugs — it detects phantom recaptures on undefended pieces while missing real recaptures from actual defenders. Additionally, one test FEN is invalid (knight on e5 cannot reach e6). The implementation structure matches the plan (correct files created/modified, correct module organization, correct integration points in ordering.rs and lib.rs), but the SEE algorithm itself is broken.","issues":[{"file":"crates/search/src/see.rs","severity":"critical","description":"SEE algorithm produces incorrect results in 7/8 tests. Systematic bug in attackers_to() or X-ray discovery logic: (1) Phantom recaptures detected on undefended pieces — e.g., PxN on undefended knight returns 220 instead of 320, BxR on undefended rook returns 170 instead of 500, RxQ on undefended queen returns 400 instead of 900, NxR on undefended rook returns 180 instead of 500. (2) Real recaptures missed — PxN with knight recapture returns 320 instead of 220, meaning the c7 knight's recapture is not detected. (3) Multi-piece exchange returns non-negative when it should be negative."},{"file":"crates/search/src/see.rs","severity":"major","description":"Test see_nxb_defended_roughly_equal uses invalid FEN: knight on e5 (index 36) cannot reach e6 (index 44) — e6 is one rank directly ahead, not a valid knight move. The test panics with 'expected capture move must exist'. The knight should be placed on a square that can actually attack e6 (e.g., d4, f4, g5, g7, d8, f8, c5, c7)."},{"file":"crates/search/src/see.rs","severity":"critical","description":"The pawn_attacks() helper function or the attackers_to() function likely has a bug in shift direction or file mask logic, causing incorrect attacker sets. The pattern of phantom recaptures (each returning captured_value minus attacker_value) strongly suggests pieces that should not be attacking the target square are being included in the attacker set."}]}