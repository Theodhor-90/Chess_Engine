- **`crates/search/src/killer.rs`** — Created. `KillerTable` struct with `new`, `store`, `is_killer`, `clear` methods. `Default` impl. 5 unit tests: `store_and_retrieve`, `slot_rotation_evicts_oldest`, `duplicate_not_shifted`, `killers_per_ply_independent`, `clear_resets_all`.
- **`crates/search/src/ordering.rs`** — Modified. Added `use crate::killer::KillerTable`, `KILLER_SCORE` constant (50), private `score_move` helper integrating MVV-LVA + killer scoring. Changed `order_moves` signature to accept `&KillerTable` and `ply`. Updated existing tests for new signature. Added `killer_moves_ordered_between_captures_and_quiet` test.
- **`crates/search/src/lib.rs`** — Modified. Added `pub mod killer` declaration, `use killer::KillerTable`. Extended `SearchContext` with `killers: KillerTable` field. Updated `search` to initialize `killers: KillerTable::new()`. Updated `negamax` to record killers on quiet beta cutoffs (`ctx.killers.store(ply, mv)`). Updated both `order_moves` call sites in `negamax` and `quiescence` to pass `&ctx.killers` and `ply`. Updated `test_ctx` helper with `killers` field.