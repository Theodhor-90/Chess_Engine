- **`crates/eval/src/phase.rs`** — Created. Defines `MAX_PHASE = 24`, phase weights (N=1, B=1, R=2, Q=4), `compute_phase` function that sums weighted piece counts for both colors and clamps to `[0, MAX_PHASE]`. Four unit tests: `startpos_phase_is_max`, `king_and_pawns_phase_is_zero`, `missing_one_queen_intermediate`, `promoted_pieces_clamped`.
- **`crates/eval/src/lib.rs`** — Modified. Added `pub mod phase;`. Removed placeholder `const MAX_PHASE` and `fn compute_phase`. Added `use phase::{compute_phase, MAX_PHASE};`. Updated `tapered_eval_interpolation` test expected value from `333` to `344` (phase now returns `1` for the one-knight position instead of placeholder `24`).