{"verdict":"needs_revision","feedback":"The plan has a critical feasibility issue with reading private constants, a contradictory/duplicate field in EvalParams, and an unused dependency. These must be resolved before the plan can be locked.","issues":[{"category":"correctness","description":"EvalParams::from_current() claims it will initialize by 'reading the public const values and public accessor functions (chess_eval::PAWN_VALUE, chess_eval::pst::mg_table(), etc.)'. However, nearly all eval constants are module-private (not pub): all pawn structure constants (DOUBLED_PAWN_PENALTY_MG, etc.), king safety constants (PAWN_SHIELD_BONUS_MG, ATTACK_WEIGHTS, etc.), mobility constants (KNIGHT_MOBILITY_MG, etc.), piece bonus constants (BISHOP_PAIR_BONUS_MG, etc.), and king centralization constants (KING_CENTER_BONUS_WEIGHT, etc.) are all declared with 'const' not 'pub const'. Only material values (PAWN_VALUE, etc.) and PST tables (MG_PAWN_TABLE, etc.) are public. The plan must specify exactly how these ~60+ private constants will be read. Options: (a) hardcode the current values directly in the tuner's EvalParams initialization, or (b) explicitly state that these constants will be made pub (which modifies the eval crate and should be listed as a file to modify). This is a blocking issue — without it, from_current() cannot be implemented as described."},{"category":"correctness","description":"EvalParams has contradictory/duplicate fields for king proximity bonuses. The 'pawn_mg: [i32; 6]' array description at line 80 says it contains 'KING_PROXIMITY_OWN_BONUS' as one of its 6 elements. But then lines 84-85 also declare separate 'king_proximity_own_bonus: i32' and 'king_proximity_opp_bonus: i32' fields. This is a contradiction — the same constants appear in two places. Additionally, the note '(Note: KING_PROXIMITY_OWN_BONUS and KING_PROXIMITY_OPP_BONUS are EG-only in usage, included as single scalar)' is inconsistent with placing them in the pawn_mg array. Fix: remove king proximity from pawn_mg/pawn_eg arrays (making them [i32; 4] each: doubled, isolated, backward, connected, blocked_passed as 5 or just the first 4), and keep only the standalone scalar fields."},{"category":"completeness","description":"The pawn_mg field is declared as [i32; 6] containing '[DOUBLED_PAWN_PENALTY_MG, ISOLATED_PAWN_PENALTY_MG, BACKWARD_PAWN_PENALTY_MG, CONNECTED_PAWN_BONUS_MG, BLOCKED_PASSED_PAWN_PENALTY_MG, KING_PROXIMITY_OWN_BONUS]' — but that's only 5 named constants plus KING_PROXIMITY_OWN_BONUS (which appears elsewhere). The actual pawn.rs has DOUBLED, ISOLATED, BACKWARD, CONNECTED, BLOCKED_PASSED (5 scalar MG constants) plus KING_PROXIMITY_OWN and KING_PROXIMITY_OPP (2 EG-only scalars). Clarify the exact mapping between EvalParams fields and eval crate constants. Specify which constants go into pawn_mg and pawn_eg arrays with their exact indices."},{"category":"scope","description":"The dependency list includes 'chess-movegen' with the justification 'provides generate_legal_moves() for filtering quiet positions (positions where no captures are available and side is not in check)'. However, nowhere in the implementation details (dataset loading, eval, optimization) is movegen actually used. The dataset file format expects pre-filtered FEN lines. Position filtering is a T02 concern (dataset preparation), not a T01 concern (tuning infrastructure). Either remove chess-movegen from the dependency list, or if it IS needed (e.g., for filtering at load time), describe its usage explicitly in the implementation details."},{"category":"ambiguity","description":"Vague term 'etc.' at: 'chess_eval::PAWN_VALUE, chess_eval::pst::mg_table(), etc.)'. Although the preceding items give examples, the full list of accessors/constants that from_current() will read is not enumerated. Given the private-vs-public issue above, this must be made explicit — list every constant source and how it will be accessed."}]}