{"verdict":"needs_revision","feedback":"The v2 plan is substantially improved — it addressed most v1 feedback (removed Stockfish, provided complete inline scripts, chose a single approach). However, there are remaining issues: the regression detection logic has the wrong statistical interpretation (it checks elo_lower < -30 where elo_lower is the lower bound of the CI, but detecting 'Elo drops by more than 30 at p<0.05' requires checking whether the UPPER bound of the CI is below -30, not the lower bound), the CuteChess-CLI build-from-source approach has a Qt dependency that may be fragile in CI, and there is a residual preamble line from the planning conversation.","issues":[{"category":"correctness","description":"The regression detection logic has the wrong direction for the confidence interval check. The spec says 'flags a regression if Elo drops by more than 30 at p<0.05'. This means we need 95% confidence that the true Elo is below -30. That requires the UPPER bound of the 95% CI to be below -30 (i.e., even the most optimistic estimate is worse than -30). The plan checks `elo_lower < -30`, which is the LOWER bound — this fires when merely the pessimistic tail is below -30, which is a much weaker criterion that would flag regressions even when the true Elo might be near zero. The comment in the plan at line 142-148 even acknowledges this confusion by saying 'if the UPPER bound of the Elo CI is below -30, we are confident at p<0.05 that the true Elo is below -30' but then immediately contradicts itself by using elo_lower. Fix: the regression check should be `elo_upper < -30` (where elo_upper = Elo computed from score + 1.96*SE, the upper/optimistic bound of the score mapped to Elo). Since score_upper > score, elo_upper > elo — so if even the optimistic Elo is below -30, we have statistical confidence the regression is real. Alternatively and equivalently: the check should be that the entire 95% CI lies below -30."},{"category":"correctness","description":"The plan's inline comment at lines 142-148 is self-contradictory. It first correctly states: 'if the UPPER bound of the Elo CI is below -30, we are confident at p<0.05 that the true Elo is below -30'. Then it says 'Using elo_lower: if elo_lower < -30, the PR engine is significantly weaker by more than 30 Elo at the 95% confidence level' — this is the opposite claim. elo_lower being below -30 means the pessimistic end of the CI is below -30, which does NOT establish p<0.05 confidence that the Elo is below -30. Reconcile this: the check must use elo_upper (the optimistic bound), not elo_lower."},{"category":"completeness","description":"The plan references 'tools/selfplay/openings.txt' in the CuteChess-CLI invocation (step 8), but does not verify that this file will be available at that relative path in the CI checkout. Since the file is part of the repo (confirmed it exists), this should work, but the plan should explicitly note that the openings file is committed to the repository and available at checkout. Minor — but the implementer should be aware."},{"category":"format","description":"Line 1 of plan-v2.md reads 'Now I have all the context. Let me produce the revised plan.' — this is a leftover preamble from the planning conversation and should be removed. The plan file should start at '## 1. Deliverables'."},{"category":"ambiguity","description":"CuteChess-CLI build-from-source step installs Qt5 via 'sudo apt-get install -y qtbase5-dev'. On recent ubuntu-latest runners (Ubuntu 22.04+), Qt5 development packages may have different names or may be deprecated in favor of Qt6. The plan pins CuteChess at v1.3.1 which uses Qt5, but should specify a fallback or note that this was verified on ubuntu-22.04. If ubuntu-latest moves to 24.04+, qtbase5-dev may not be available. Consider pinning the runner to 'ubuntu-22.04' instead of 'ubuntu-latest' to ensure stability, or document the assumption."}]}