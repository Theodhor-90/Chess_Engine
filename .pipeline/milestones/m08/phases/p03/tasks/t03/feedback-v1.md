{"verdict":"needs_revision","feedback":"The plan has a well-justified head-to-head design deviation but contains several ambiguities, missing details, and a scope concern that need resolution before locking.","issues":[{"category":"ambiguity","description":"CuteChess-CLI download step is vague: 'Fetch the CuteChess-CLI Linux binary from the CuteChess GitHub releases (or build from source via apt-get install cutechess)'. Specify the exact approach — which GitHub release URL/tag, or which apt package — so the implementer does not have to guess. Provide the concrete download URL pattern (e.g., 'https://github.com/cutechess/cutechess/releases/download/v1.3.1/cutechess-cli-linux-x86_64') or specify 'build from source using apt-get install qt5-default && cmake/make' with exact steps."},{"category":"ambiguity","description":"Stockfish download step is vague: 'Fetch a Stockfish Linux binary from the official Stockfish GitHub releases'. Specify the exact release URL pattern, version/tag, and binary variant (e.g., 'stockfish-ubuntu-x86-64-avx2' from 'https://github.com/official-stockfish/Stockfish/releases/download/sf_17/...'). Also specify how Stockfish is constrained to play at reduced strength — the plan mentions 'option.\"Skill Level\"=5' in one place but this is buried in an example CuteChess-CLI invocation and never formally specified."},{"category":"ambiguity","description":"Time control '5+0.05' appears in the example CuteChess-CLI invocation but is never formally declared as the chosen time control. Specify the exact time control for the 100-game CI tournament (e.g., 'tc=5+0.05' meaning 5 seconds + 50ms increment, or a different value) and justify the choice for CI runtime feasibility."},{"category":"ambiguity","description":"The plan oscillates between approaches without settling on one. Step 8 says 'Execute two 100-game tournaments... PR engine vs Stockfish (100 games), Baseline engine vs Stockfish (100 games)' then immediately says 'Alternatively, run a single head-to-head tournament: PR engine vs Baseline engine (100 games)... This is the preferred approach.' Deviation 1 confirms head-to-head is chosen. Remove the non-preferred approach entirely from the implementation details to eliminate confusion for the implementer."},{"category":"ambiguity","description":"Vague phrase 'as a fallback reference engine in case head-to-head produces inconclusive results' in Deviation 1. Define 'inconclusive' concretely (e.g., 'if all 100 games are draws') and specify what the fallback entails (run PR-vs-Stockfish 100 games instead? in addition?). If the fallback is not truly needed, remove Stockfish from the workflow to simplify."},{"category":"completeness","description":"The regression detection shell script logic is described in prose but never provided as concrete code. The plan must include the exact shell script (or pseudocode precise enough to translate 1:1) for: parsing the CuteChess-CLI 'Score of' line, computing Elo, computing the 95% CI, and performing the threshold check. The verification commands section includes a partial snippet but it does not cover CI parsing or the full threshold check. The implementer needs the complete script."},{"category":"completeness","description":"Missing detail: how is the opening book/positions file specified for the head-to-head tournament? The plan references 'tools/selfplay/openings.txt' in one CuteChess-CLI example but never formally declares this as the opening source for the CI tournament. Specify the exact -openings flag and format (e.g., '-openings file=tools/selfplay/openings.txt format=epd order=random')."},{"category":"completeness","description":"Missing detail: the exact CuteChess-CLI command line for the head-to-head PR-vs-Baseline tournament is never provided. Only the PR-vs-Stockfish example is given with specific flags. Provide the concrete CuteChess-CLI invocation for the chosen head-to-head approach including all flags: engine names, cmd paths, proto, tc, games, rounds, repeat, concurrency, openings, pgnout, etc."},{"category":"correctness","description":"The Elo formula in the plan uses natural log but writes 'log10': 'Elo = -400 * log10(1/score - 1)'. The verification command uses 'l()' (natural log) divided by 'l(10)' which is correct for log base 10. However, the standard Elo difference formula is: Elo = -400 * log10(score / (1 - score)). Verify that the formula sign convention is consistent — with the PR engine as 'player 1', a score < 0.5 should yield negative Elo (regression). The formula as written: -400 * log10(1/score - 1) = -400 * log10((1-score)/score) = 400 * log10(score/(1-score)), which gives positive Elo for score > 0.5. Clarify sign convention explicitly so the implementer gets the threshold check direction correct."},{"category":"correctness","description":"The SE-to-Elo conversion is imprecise. The plan says 'Convert SE to Elo error margin for 95% CI: compute elo_lower from score - 1.96*se'. But the Elo formula is nonlinear — subtracting 1.96*SE from the score and converting to Elo is an approximation (delta method). This is acceptable for 100 games but should be explicitly acknowledged. More importantly, the plan must specify what happens at edge cases: if score = 0 or score = 1, the log formula is undefined. Add handling for these edge cases (e.g., if score = 0 → always flag regression; if score = 1 → always pass)."},{"category":"scope","description":"The plan downloads Stockfish 'as a fallback reference engine' even though the chosen approach is head-to-head PR-vs-Baseline. The task spec says 'runs a 100-game tournament after each PR and flags a regression if Elo drops by more than 30 at p<0.05' — it does not require downloading reference engines for the CI regression job (that was T01's domain). Unless the fallback is concretely defined and justified, downloading Stockfish in CI is unnecessary scope. Either define the fallback precisely or remove the Stockfish download step."}]}